//
//浮動小数点の最大値と最小値を表示
//

#include <stdio.h>
#include <float.h>

int main()
{
 printf("FLT_MIN = %e,  FLT_MAX =%e\n", FLT_MIN, FLT_MAX);
  printf("DBL_MIN = %e,  DBL_MAX =%e\n", DBL_MIN, DBL_MAX);

  return 0;
}